# ML experiments

Training notebooks for production models live under `datasets/`:

- `datasets/entsoe/` — ML1 electricity price forecast
- `datasets/apsi/` — ML2 gas storage risk

Use this folder for one-off experiments or homework notebooks that are not part of the deployed app.
